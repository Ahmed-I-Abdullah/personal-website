import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ff8906",
    },
    secondary: {
      main: "#FFFFFF",
    },
    text: {
      primary: "#000",
      secondary: "#79797B",
    },
    background: {
      default: "#1A1A1D",
    },
  },
});